import React from 'react';
import Layout from '@theme/Layout';

export default function Introduction() {
  return (
    <Layout title="Introduction" description="Página de Introdução em React">
      <div style={{ padding: '50px', maxWidth: '1000px', margin: '0 auto' }}>
        <h1 style={{ color: '#6a0dad' }}>Introduction</h1>
        <p>Este conteúdo agora é 100% React!</p>
        
        {/* Você pode colocar qualquer lógica React aqui */}
        <button onClick={() => alert('Java is UnderControl!')}>
          Clique aqui
        </button>
      </div>
    </Layout>
  );
}